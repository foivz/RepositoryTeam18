<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="Desktop_Velpro.Glavna_forma"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Desktop_Velpro"
    xmlns:common="using:Desktop_Velpro.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>

        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Style="{StaticResource LayoutRootStyle}">
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="{StaticResource Crvena_gore}" Offset="0.4"/>
                <GradientStop Color="{StaticResource Crvena_dolje}" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:ConnectionId='8' x:Name="backButton"                IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}" Style="{StaticResource BackButtonStyle}"/>
            <TextBlock x:Name="pageTitle" Grid.Column="1" Text="Glavna Forma" Style="{StaticResource PageHeaderTextStyle}">
                <TextBlock.Foreground>
                    <SolidColorBrush Color="{StaticResource Zuta_tekst}"/>
                </TextBlock.Foreground>
            </TextBlock>
        </Grid>
        <Button x:ConnectionId='1' x:Name="Pregled_akt" Content="Pregled aktivnih narudžbi" HorizontalAlignment="Left" Margin="34,106,0,0" Grid.Row="1" VerticalAlignment="Top" Foreground="Black" Height="175" Width="294" RenderTransformOrigin="0.5,0.5" FontSize="20"                          >
            <Button.Projection>
                <PlaneProjection/>
            </Button.Projection>
            <Button.RenderTransform>
                <CompositeTransform/>
            </Button.RenderTransform>
            <Button.Background>
                <SolidColorBrush Color="{StaticResource Zuta_tekst}"/>
            </Button.Background>
        </Button>
        <Button x:ConnectionId='2' x:Name="Pregled_arh" Content="Pregled arhive narudžbi" HorizontalAlignment="Left" Margin="34,354,0,0" Grid.Row="1" VerticalAlignment="Top" Foreground="Black" Height="175" Width="294" RenderTransformOrigin="0.5,0.5" FontSize="20"                          >
            <Button.Projection>
                <PlaneProjection/>
            </Button.Projection>
            <Button.RenderTransform>
                <CompositeTransform/>
            </Button.RenderTransform>
            <Button.Background>
                <SolidColorBrush Color="{StaticResource Zuta_tekst}"/>
            </Button.Background>
        </Button>
        <Button x:ConnectionId='3' x:Name="Pregled_sklad" Content="Pregled stanja na skladištu" HorizontalAlignment="Left" Margin="368,106,0,0" Grid.Row="1" VerticalAlignment="Top" Foreground="Black" Height="175" Width="294" RenderTransformOrigin="0.5,0.5" FontSize="20"                            >
            <Button.Projection>
                <PlaneProjection/>
            </Button.Projection>
            <Button.RenderTransform>
                <CompositeTransform/>
            </Button.RenderTransform>
            <Button.Background>
                <SolidColorBrush Color="{StaticResource Zuta_tekst}"/>
            </Button.Background>
        </Button>
        <Button x:ConnectionId='4' x:Name="Pregled_zelje" Content="Pregled liste želja" HorizontalAlignment="Left" Margin="368,354,0,0" Grid.Row="1" VerticalAlignment="Top" Foreground="Black" Height="175" Width="294" RenderTransformOrigin="0.5,0.5" FontSize="20"                            >
            <Button.Projection>
                <PlaneProjection/>
            </Button.Projection>
            <Button.RenderTransform>
                <CompositeTransform/>
            </Button.RenderTransform>
            <Button.Background>
                <SolidColorBrush Color="{StaticResource Zuta_tekst}"/>
            </Button.Background>
        </Button>
        <Button x:ConnectionId='5' x:Name="Naruciti_dob" Content="Naručiti od dobavlača" HorizontalAlignment="Left" Margin="699,106,0,0" Grid.Row="1" VerticalAlignment="Top" Foreground="Black" Height="175" Width="289" RenderTransformOrigin="0.5,0.5" FontSize="20"                           >
            <Button.Projection>
                <PlaneProjection/>
            </Button.Projection>
            <Button.RenderTransform>
                <CompositeTransform/>
            </Button.RenderTransform>
            <Button.Background>
                <SolidColorBrush Color="{StaticResource Zuta_tekst}"/>
            </Button.Background>
        </Button>
        <Button x:ConnectionId='6' x:Name="Statistike" Content="Statistike o narudžbama" HorizontalAlignment="Left" Margin="699,354,0,0" Grid.Row="1" VerticalAlignment="Top" Foreground="Black" Height="175" Width="294" RenderTransformOrigin="0.5,0.5" FontSize="20"                         >
            <Button.Projection>
                <PlaneProjection/>
            </Button.Projection>
            <Button.RenderTransform>
                <CompositeTransform/>
            </Button.RenderTransform>
            <Button.Background>
                <SolidColorBrush Color="{StaticResource Zuta_tekst}"/>
            </Button.Background>
        </Button>
        <Button x:ConnectionId='7' x:Name="Azuriraj" Content="Ažuriraj zaposlenike" HorizontalAlignment="Left" Margin="1035,106,0,0" Grid.Row="1" VerticalAlignment="Top" Foreground="Black" Height="423" Width="289" RenderTransformOrigin="0.5,0.5" FontSize="20"                       >
            <Button.Projection>
                <PlaneProjection/>
            </Button.Projection>
            <Button.RenderTransform>
                <CompositeTransform/>
            </Button.RenderTransform>
            <Button.Background>
                <SolidColorBrush Color="{StaticResource Zuta_tekst}"/>
            </Button.Background>
        </Button>

        <VisualStateManager.VisualStateGroups>

            <!-- Visual states reflect the application's view state -->
            <VisualStateGroup x:Name="ApplicationViewStates">
                <VisualState x:Name="FullScreenLandscape"/>
                <VisualState x:Name="Filled"/>

                <!-- The entire page respects the narrower 100-pixel margin convention for portrait -->
                <VisualState x:Name="FullScreenPortrait">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PortraitBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>

                <!-- The back button and title have different styles when snapped -->
                <VisualState x:Name="Snapped">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="pageTitle" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedPageHeaderTextStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</common:LayoutAwarePage>

